// THIS COMPONENT HAS ONE SINGLE TASK.
// one line...automaticaly returns. all this does is return what's passing between the opening and closing tag in the cockpit.js file in render method.

const aux = (props) => props.children;

export default aux;
